# Ce fichier sert de config pnpm pour apps/web (workspace mono-paquet).
packages:
  - '.'

# pnpm >= 11 : minimumReleaseAge (anti supply-chain) bloque les paquets < 24h.
# Désactivé en dev ; à re-durcir (ex. 10080 = 7 jours) une fois les deps stabilisées.
minimumReleaseAge: 0
