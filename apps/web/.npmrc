# pnpm >= 11 : la politique minimum-release-age (anti supply-chain attack)
# bloque les paquets publiés < 24h. Désactivée en dev pour ne pas casser
# les installs sur lockfile frais ; à re-durcir (ex. 10080 = 7 jours)
# une fois les dépendances stabilisées.
minimum-release-age=0
